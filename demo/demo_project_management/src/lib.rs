// If you have lib.rs defined in the source directory,
// then a library crate with the same name as the package will be automatically created,
// and lib.rs will be the crate root.

// The crate root is the source file that the Rust compiler starts at
// when building the crate.

// REF: https://www.youtube.com/watch?v=5RPXgDQrjio

fn main() {
    // If you have main.rs defined in the source directory,
    // then a binary crate with the same name as the package will be automatically created,
    // and main.rs will be the crate root.

    // The crate root is the source file that the Rust compiler starts at
    // when building the crate.
}
